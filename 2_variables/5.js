// This program would return 'bar' because it is declared outside of the block and the console.log is also outside of the block and doesn't have access to inside where the foo variable is given a different value. 