// '12' < '9' evaluates as true since the operands are strings. When you compare two strings, JS performs a character by character comparison from left to right, so the first comparison is actually '1' < '9' 